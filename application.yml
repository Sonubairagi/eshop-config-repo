spring:
  application:
    name: ESHOP-APPLICATION

server:
  port: 8080

#Eureka/Registry/Discovery Client Config
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
